// app/admin/page.js
export default function AdminPage() {
    return (
        <div>
            <h1>Admin</h1>
            <p>Panel de administración (ej. gestión de productos, usuarios, etc.)</p>
        </div>
    );
}

